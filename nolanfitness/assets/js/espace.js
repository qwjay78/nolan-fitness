document.addEventListener('DOMContentLoaded', function() {
    // Récupérer le nom de l'utilisateur connecté (simulé ici)
    const username = 'John Doe'; // Remplacer par le nom de l'utilisateur réel

    // Mettre à jour le contenu du span avec le nom de l'utilisateur
    const usernameSpan = document.getElementById('username');
    usernameSpan.textContent = username;


});
